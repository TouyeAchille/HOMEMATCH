name: HomeMatch

conda_env: conda.yaml

entry_points:
    main:
      parameters:
        hydra_options:
          description: configuration parameters to ovveride
          type: string
          default: ''
      command: python main.py  $(echo {hydra_options})
